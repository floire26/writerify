<%-include("./_partials/header")-%>

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" type="text/css" href="//fonts.googleapis.com/css?family=Nunito" />
  <title>List of Courses</title>
  <style>
    .container {
      width: 65rem;
      margin: 0 auto;
      padding: 1rem;
    }
    .title {
      font-size: 48px;
      font-family: Nunito;
      font-style: normal;
      font-variant: normal;
      font-weight: 800;
      line-height: 26.4px;
      margin-bottom: 1rem;
      margin-top: 1rem;
    }
    .subtitle {
      font-size: 24px;
      margin: 1rem 0;
      font-size: 24px;
      font-family: Nunito;
      font-style: normal;
      font-variant: normal;
      font-weight: 400;
      line-height: 26.4px;
      margin-bottom: 1rem;
    }
    .cards {
      display: flex;
      justify-content: space-between;
      width: 100%;

      flex-wrap: wrap;
      padding: 1rem 0;
      gap: 2rem;
    }

    .card {
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      gap: 1rem;
      background-color: #f9f5e7;
      border-radius: 1rem;
      padding: 1rem;
      width: 31%;
    }

    .card-content {
      display: flex;
      flex-direction: column;
      gap: 1rem;
      font-size: 18px;
      font-family: Nunito;
      font-style: normal;
      font-variant: normal;
      font-weight: 400;
      line-height: 26.4px;
      margin-bottom: 1rem;
    }

    .card-title {
      text-align: center;
      font-size: 24px;
      font-family: Nunito;
      font-style: normal;
      font-variant: normal;
      font-weight: 800;
      line-height: 26.4px;
      margin-bottom: 1rem;
    }

    .card-detail {
      display: flex;
      gap: 2rem;
      font-family: Nunito;
      font-style: normal;
      font-variant: normal;
      font-weight: 400;
      line-height: 26.4px;
      margin-bottom: 1rem;
    }

    .card button {
      width: 100%;
      margin-top: 1em;
      background-color: #a7727d;
      color: white;
      border: none;
      padding: 0.5em;
      border-radius: 0.5em;
      cursor: pointer;
      transition-duration: 200ms;
    }

    button {
      background-color: #a7727d;
      color: white;
      border: none;
      padding: 0.5em;
      border-radius: 0.5em;
      cursor: pointer;
      transition-duration: 200ms;
    }

    button:hover {
      background-color: #b56373;
    }

    .bottom-action {
      margin-top: 2rem;
      display: inherit;
      text-align: center;
    }

    .bottom-action button {
      font-size: 20px;
      padding: 0.5em 1em;
    }

    button #unenroll {
      font-size: 20px;
      padding: 0.5em 1em;
      background-color: #7a5461;
    }

    button #unenroll:hover {
      background-color: #0f0c0d;
    }

    .card form{
      line-height: 26.4px;
      margin-bottom: 1rem;
    }

    .stripe-button-el { display: none }

    form input#search {
      line-height: 30px;
      padding: 6px;
      width: 89%;
    }
  </style>
</head>

<body>
  <div class="container">
    <h1 class="title">List of <%= prof? "Recommended" : "" %> Courses</h1>

    <% if (prof) { %>
    <p class="subtitle">
      As a/an <%= courses[0].Proficiency.level %> student, here are some courses
      that you might be interested...
    </p>
    <% } else { %>
    <p class="subtitle">
      Here are all of the courses that are being offered by Writerify...
      <form action="" method="get" class="bottom-action">
        <input type="text" name="search" id="search"/>
        <button type="submit">Submit</button>
      </form>
    </p>
    <% } %>

    <% if (courses.length === 0) { %>
        <p class="subtitle">No courses are found!</p>
    <% } else { %>
        <div class="cards">
        <% courses.forEach(course => { %>
        <div class="card">
            <div class="card-content">
            <h1 class="card-title"><%= course.name %></h1>
            <p class="card-description"><%= course.description %></p>
            </div>
            <div>
            <div class="card-detail">
                <p><strong>Duration</strong><br /><%= course.duration %></p>
                <p><strong>Level</strong><br /><%= displayCourseLevel(course.Proficiency.level) %></p>
                <p><strong>Price</strong><br /><%= course.convertPrice() %></p>
            </div>
            <% if (user.Courses.every(enrolledCourse => enrolledCourse.name !==
            course.name)) { %>
            <form
                action="/courses/<%= user.id %>/enroll/<%= course.id %>"
                method="post"
            >
                <a href="/courses/<%= user.id %>/enroll/<%= course.id %>" class="bottom-action" >
                <script
                    src="//checkout.stripe.com/v2/checkout.js"
                    class="stripe-button"
                    data-key="<%= key %>"
                    data-amount="<%= course.price * 100%>"
                    data-label="Enroll"
                    data-currency="usd"
                    data-name="<%= user.username %>"
                    data-description="Checkout for enrolling in <%= course.name %>"
                    data-locale="auto"
                ></script>
                  <button >Enroll</button>
                </a>
            </form>
            <% } else { %>
            <a class="bottom-action" href="/courses/<%= user.id %>/unenroll/<%= course.id %>" id="unenroll">
                <button id="unenroll" >Unenroll</button>
            </a>
            <% } %>
            </div>
        </div>
        <% }); %>
        </div>
    <% } %>

    <% if (prof) { %>
    <a class="bottom-action" href="/courses/<%= user.id %>">
      <button>Let me see all of the courses!</button>
    </a>
    <% } else { %>
    <a
      class="bottom-action"
      href="/courses/<%= user.id %>?prof=<%= user.ProficiencyId %>"
    >
      <button>Give me courses recommendation!</button>
    </a>
    <% } %>

  </div>
</body>
